version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: get_ip_service
    ports:
      - "8001:8001"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
    # Optional: Add resource limits
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '0.5'
    #       memory: 512M
    #     reservations:
    #       cpus: '0.25'
    #       memory: 256M
